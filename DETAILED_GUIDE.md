# mem1系统技术指南 

详细的架构、设计、配置和扩展文档。

[📚 返回主README](README.md)

## 目录

- [项目结构](#项目结构)
- [核心流程详解](#核心流程详解)
- [三层记忆架构](#三层记忆架构)
- [配置说明](#配置说明)
- [自定义扩展](#自定义扩展)
- [故障排查](#故障排查)

---

## 项目结构

```
memory-system/
├── memory-system-core/                    # 核心模块（基础类、异常、模型）
│   └── src/main/java/com/memosystem/
│       ├── common/
│       │   ├── exception/                 # 自定义异常
│       │   │   ├── MemorySystemException.java
│       │   │   ├── LLMClientException.java
│       │   │   ├── MemoryOperationException.java
│       │   │   ├── StorageException.java
│       │   │   └── JsonParseException.java
│       │   └── model/
│       │       ├── CommonFileRepository.java   # 文件持久化
│       │       ├── ParsedMessage.java          # 解析后消息
│       │       └── Result.java                 # 统一响应
│       └── core/
│           ├── conversation/
│           │   └── MessagePair.java            # 消息对
│           ├── memory/
│           │   ├── MemoryEntry.java            # 记忆条目
│           │   ├── CandidateMemory.java        # 候选记忆
│           │   └── MemorySimilarity.java       # 记忆相似度
│           └── summary/
│               └── GlobalSummaryEntry.java     # 全局摘要条目
│
├── memory-system-spring-boot-starter/     # Spring Boot Starter（核心实现）
│   └── src/main/java/com/memosystem/
│       ├── adapter/
│       │   ├── llm/
│       │   │   ├── LLMClientInterface.java     # LLM 接口
│       │   │   ├── LLMClient.java              # LLM 实现
│       │   │   └── LLMConfig.java              # 4个 LLM Bean 配置
│       │   └── storage/
│       │       └── QdrantLocalClient.java      # 向量数据库
│       │
│       ├── config/
│       │   ├── MemorySystemProperties.java     # 记忆系统配置
│       │   ├── MemorySystemAutoConfiguration.java
│       │   ├── ThreadPoolConfig.java           # 线程池配置
│       │   ├── MemoryPrompts.java              # 提示词模板
│       │   ├── SwaggerConfig.java              # API 文档配置
│       │   ├── GlobalExceptionHandler.java     # 全局异常处理
│       │   ├── TimingAspect.java               # 耗时监控切面
│       │   └── TimingMonitor.java              # 耗时监控注解
│       │
│       ├── controller/
│       │   └── ConversationController.java     # REST API
│       │
│       ├── service/
│       │   ├── ConversationService.java        # 对话服务接口
│       │   ├── ConversationEnhancerService.java # 对话增强服务接口
│       │   ├── ShortTermMemoryService.java     # 短期记忆服务接口
│       │   ├── EmbeddingService.java           # Embedding 接口
│       │   ├── FactExtractorService.java       # 事实抽取服务接口
│       │   ├── GlobalSummaryService.java       # 全局摘要服务接口
│       │   ├── MemoryRetrieverService.java     # 记忆检索服务接口
│       │   ├── MemoryUpdateService.java        # 记忆更新服务接口
│       │   ├── MessageProcessService.java      # 消息处理服务接口
│       │   └── impl/
│       │       ├── ConversationServiceImpl.java        # 对话服务实现
│       │       ├── ConversationEnhancerServiceImpl.java # 对话增强实现
│       │       ├── ShortTermMemoryServiceImpl.java     # 短期记忆实现（懒加载）
│       │       ├── MessageUpdateStage.java             # 记忆决策（ADD/UPDATE/DELETE/NOOP）
│       │       ├── FactExtractorServiceImpl.java       # 事实抽取实现
│       │       ├── MemoryUpdateServiceImpl.java        # 记忆更新实现
│       │       ├── GlobalSummaryServiceImpl.java       # 全局摘要实现
│       │       ├── MemoryRetrieverServiceImpl.java     # 记忆检索实现
│       │       ├── MessageProcessServiceImpl.java      # 消息处理实现
│       │       └── DefaultEmbeddingService.java        # 默认伪 Embedding
│       │
│       ├── dto/
│       │   ├── ChatRequestDTO.java             # 聊天请求 DTO
│       │   └── UpdateSystemContextDTO.java     # 更新上下文请求 DTO
│       │
│       └── vo/
│           ├── PromptResponseVO.java           # 提示词响应 VO
│           ├── ConversationResultVO.java       # 对话结果 VO
│           └── VersionInfoVO.java              # 版本信息 VO
│
├── memory-system-example/                 # 示例应用
│   └── src/main/resources/
│       └── application.yaml               # 示例配置
│
├── README.md                              # 快速入门指南
├── README_EN.md                           # 英文版 README
├── DETAILED_GUIDE.md                      # 本文档（技术详细指南）
└── pom.xml
```

---

## 核心流程详解

### processConversation 主流程

```
用户消息
   │
   ▼
┌─────────────────────────────────────────┐
│ 阶段 1：构建增强提示词                     │
│ buildPromptWithMemories()               │
│                                         │
│ ├─ 步骤1：解析用户消息                     │
│ ├─ 步骤2：向量检索（长期语义记忆）           │
│ ├─ 步骤3：获取短期记忆上下文                │
│ ├─ 步骤4：获取全局摘要                     │
│ └─ 步骤5：组装增强提示词                   │
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│ 阶段 2：获取AI响应                        │
│ conversationEnhancerService.getAIResponse()
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│ 阶段 3：更新系统上下文                     │
│ updateSystemContext()                   │
│                                         │
│ ├─ 步骤5：保存消息到持久化+短期缓存         │
│ ├─ 步骤6：获取上下文                      │
│ ├─ 步骤6.5：创建临时摘要                  │
│ ├─ 并行任务：                            │
│ │   ├─ 步骤7：抽取候选记忆（LLM）          │
│ │   └─ 步骤9：更新全局摘要（LLM）          │
│ └─ 步骤8：更新记忆库（决策+向量存储）        │
└─────────────────────────────────────────┘
   │
   ▼
 返回结果
```

### 记忆决策流程 (MessageUpdateStage)

```
候选记忆
   │
   ▼
┌─────────────────────────────────────────┐
│ 1. 获取候选记忆的向量表示                  │
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│ 2. 向量数据库搜索相似的已有记忆             │
└─────────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────────┐
│ 3. LLM 决策                              │
│    输入：候选记忆 + 相似旧记忆              │
│    输出：操作类型                         │
└─────────────────────────────────────────┘
   │
   ├─→ ADD：    添加新记忆
   ├─→ UPDATE： 更新现有记忆  
   ├─→ DELETE： 删除矛盾记忆
   └─→ NOOP：   不操作
```

---

## 三层记忆架构

```
┌─────────────────────────────────────────────────┐
│                  全局摘要                        │
│         (用户画像概述，永久更新)                    │
│         GlobalSummaryService                    │
├─────────────────────────────────────────────────┤
│              长期语义记忆                        │
│    (提取的事实/偏好，向量检索，永久存储)            │
│    QdrantLocalClient + MemoryUpdateService      │
├─────────────────────────────────────────────────┤
│              短期工作记忆                        │
│    (最近N轮对话原文，内存缓存，快速读取)            │
│    ShortTermMemoryService（懒加载恢复）          │
└─────────────────────────────────────────────────┘
```

| 层级 | 存储内容 | 生命周期 | 检索方式 | 服务实现 |
|------|---------|---------|---------|---------|
| 短期记忆 | 最近N轮对话原文 | Session级别 | 直接读取 | ShortTermMemoryServiceImpl |
| 长期记忆 | 提取的事实/偏好 | 永久 | 向量相似度 | QdrantLocalClient |
| 全局摘要 | 用户画像概述 | 永久但会更新 | 直接读取 | GlobalSummaryServiceImpl |

---

## 配置说明

### application.yaml 完整配置

```yaml
# LLM 配置
llm:
  api-key: ${LLM_API_KEY:your-api-key}
  api-url: ${LLM_API_URL:https://api.deepseek.ai/v1}
  default-model: deepseek-v3
  memory-extraction-model: deepseek-v3
  decision-model: deepseek-v3
  global-memory-model: deepseek-v3
  connect-timeout: 10
  api-timeout: 30
  chat-temperature: 0.7
  memory-extraction-temperature: 0.0
  decision-temperature: 0.0
  max-tokens: 2000

# 记忆系统配置
memory:
  conversation-search-top-k: 3
  update-search-top-k: 5
  short-term-memory-size: 10      # 短期记忆容量（轮数）
  db-path: ./qdrant
  collections-dir: collections
  collection-name: memories
  api:
    prefix: /api/conversation
  thread-pool:
    core-size: 5
    max-size: 10
    keep-alive-seconds: 60
    queue-capacity: 100
```

### 4个 LLM 客户端

| Bean 名称 | 用途 | 对应配置 |
|-----------|------|---------|
| defaultLLMClient | 普通AI对话 | llm.default-model |
| memoryExtractionClient | 记忆事实提取 | llm.memory-extraction-model |
| decisionLLMClient | 记忆决策（ADD/UPDATE/DELETE） | llm.decision-model |
| globalSummaryClient | 全局摘要优化 | llm.global-memory-model |

---

## 自定义扩展

### 1. 自定义 Embedding 服务（生产必须）

默认实现是伪 Embedding（基于哈希），生产环境需替换：

```java
@Bean
public EmbeddingService embeddingService() {
    return text -> openAIClient.createEmbedding(text);
}
```

### 2. 自定义 LLM 客户端

实现 `LLMClientInterface` 接口：

```java
@Bean
public LLMClientInterface customLLMClient() {
    return new MyCustomLLMClient();
}
```

### 3. 替换向量数据库

当前使用本地文件模拟 Qdrant，可替换为：
- 远程 Qdrant 服务
- Milvus
- Pinecone
- Weaviate

---

## 故障排查

### 1. NoClassDefFoundError: ProceedingJoinPoint

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId>
</dependency>
```

### 2. TypeToken 泛型错误

确保 Gson >= 2.10.1

### 3. 记忆检索效果不理想

默认 Embedding 是伪实现，需替换为真实 API

### 4. 短期记忆丢失

正常行为，首次访问时会从 messages.json 懒加载恢复

### 5. LLM 调用超时

```yaml
llm:
  connect-timeout: 30
  api-timeout: 60
```

---

[📚 返回主README](README.md)
